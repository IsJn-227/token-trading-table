# Default to text with LF in repo
* text=auto eol=lf

# Keep Windows scripts CRLF in working copy
*.ps1 text eol=crlf
*.bat text eol=crlf

# Keep images/binaries as-is
*.png binary
*.jpg binary
*.ico binary

# Keep snapshots/diffs as binary
docs/screens/** binary
